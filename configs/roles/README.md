# Role Files

Place AI trader role markdown files here.

The current role package already exists under:

```text
roles_md/configs/roles/
```

When wiring the backend, either copy those files into this folder or configure the role loader to read from the existing package path.

